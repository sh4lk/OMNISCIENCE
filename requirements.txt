# OMNISCIENCE — Core dependencies
numpy>=1.24
scipy>=1.10
sympy>=1.12
z3-solver>=4.12
torch>=2.0
typer[all]>=0.9
customtkinter>=5.2
rich>=13.0
psutil>=5.9
fpylll>=0.6

# Optional: GPU acceleration (uncomment if CUDA available)
# cupy-cuda12x>=12.0
# pycuda>=2022.2

# Optional: Distributed computing
# ray>=2.9

# Optional: SageMath bridge (install SageMath separately)
# sage is not pip-installable — see https://www.sagemath.org
